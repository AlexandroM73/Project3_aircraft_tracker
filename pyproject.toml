[project]
name = "aircraft-tracker"
version = "1.0.0"
description = "Трекер самолётов: OpenSky API + Nominatim, PostgreSQL"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "psycopg2-binary>=2.9.9",
]

[project.optional-dependencies]
dev = ["coverage>=7.0"]
